terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
    azuread = {
      source  = "hashicorp/azuread"
      version = "~> 2.53.0"
    }
  }
  backend "azurerm" {
    resource_group_name  = "netappcdmt-tf-rg"
    storage_account_name = "netappcdmttfstac"
    container_name       = "netappcdmt-tf"
    key                  = "common/terraform.tfstate"
  }
}

provider "azurerm" {
  features {}
  resource_provider_registrations = "none"
}
